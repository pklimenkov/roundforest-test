<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
</head>

<body>
<table>
  <tr>
    <th>#</th>
    <th>Name</th>
    <th>Books</th>
  </tr>
  <% @items.each_with_index do |item, index| %>
      <tr>
        <td>
          <%= index + 1 %>.
        </td>
        <td>
          <%= item.name %>
        </td>
        <td>
          <% item.top_books(5).each_with_index do |book, book_index| %>
            <%= book_index + 1 %>. <%= book.name %>
            <br>
          <% end %>
        </td>
      </tr>
  <% end %>
</table>
</body>
</html>
